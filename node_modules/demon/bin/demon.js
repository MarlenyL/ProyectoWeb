#!/usr/bin/env node

'use strict'

let parseargs = require('parseargv')

let demon = require('../index')
,   args = process.argv.slice(2)
console.log(parseargs(args))
if( ~args.indexOf('kill') )
  demon.kill()
else if( args.length ) 
  demon.start(...parseargs(args))
else
  console.log('beep')
