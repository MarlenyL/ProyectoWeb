git extends layout

block content
    include header
    div#container
    div(class= 'Formulario')
    form(method ="post")
        label(for="user" class="form") Usuario
        input#user(type="text", name="Usuario" , required, placeholder="Usuario", maxlength="8")
        label(for="user") Contraseña
        input#password(type="password" name="Contraseña", required, placeholder="Contrseña")
        div#lower       
        input(type="submit" value="Acceder")
    footer